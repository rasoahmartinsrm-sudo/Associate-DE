# conftest.py
# Pytest configuration for the health data pipeline test suite.
# The SparkSession fixture is defined in test_contracts.py with session scope,
# so it is created once and shared across all tests.
